/**
 * Twitter/X link-preview card — reuses the same render pipeline as
 * the Open Graph image so the two previews stay identical.
 */

export { default, size, contentType, alt } from "./opengraph-image";
